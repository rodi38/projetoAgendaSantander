package teste;

import controller.Agenda;
import enums.TipoContato;
import enums.TipoEndereco;
import model.Contato;
import model.Endereco;
import model.Telefone;

import java.util.ArrayList;
import java.util.List;

public class Teste {
    public static void main(String[] args) {
        /*List<Telefone> telefones = List.of(new Telefone("85", "99283934"), new Telefone("45", "2312414532"));
        List<Endereco> enderecos = List.of(new Endereco(TipoEndereco.Parente, "", "", "", "", ""));
        Contato contato = new Contato(TipoContato.Pessoal,"rODRIGO", "Rocha", enderecos, telefones );
        Agenda agenda = new Agenda();
        agenda.adicionar(contato);
        System.out.println(agenda.exibirInfoContato(contato));*/
    }
}
